<% if obj.errors.any? %>
<div class="card mb-3">
  <div class="card-body ">
    <h4><%= pluralize(obj.errors.count, "error") %> prohibited this object from being saved:</h4>
    <ul class="text-danger">
      <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>